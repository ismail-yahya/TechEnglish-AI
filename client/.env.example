# ==============================================
# Langflow AI — Client Environment Variables
# ==============================================

# Backend API URL (required for production)
# In development: leave empty (Vite proxy handles /api → localhost:5000)
# In production: set to your Koyeb backend URL
VITE_API_URL=https://your-backend.koyeb.app/api
